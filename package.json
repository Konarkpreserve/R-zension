{
  "name": "r-zension",
  "version": "0.1.0",
  "private": true,
  "description": "Competitive Growth Platform with Intelligent Performance Insights.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install": "npm install --workspaces --include-workspace-root --ignore-scripts",
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "format": "npm run format --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
