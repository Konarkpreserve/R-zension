import { FastifyReply, FastifyRequest } from 'fastify';

export async function sampleMiddleware(_request: FastifyRequest, _reply: FastifyReply): Promise<void> {
  // Placeholder for request middleware processing
}
